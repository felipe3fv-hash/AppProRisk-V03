[pytest]
testpaths = tests
addopts = -q
# Sem esta linha, o comando `pytest` (usado pela CI) não encontra o pacote `spda`:
# ao contrário de `python -m pytest`, ele NÃO coloca o diretório atual no sys.path.
# Foi assim que a suíte passou local e quebrou na CI.
pythonpath = .
